# Program versioning

Version 1 is the initial draft. Published versions are immutable. A replacement draft copies the published configuration and receives the next version number. Old published versions remain as `SUPERSEDED` records.
