# Wallet asset delivery

W2-approved assets and visual themes feed Apple and Google rendering. The main grid continues to use only `FILLED` and `EMPTY`. No star, gift, number, check-mark replacement, badge, or milestone is introduced into the grid.

Google-compatible public progress images use random immutable tokens and immutable cache headers. Tokens contain no PII or internal identity. Apple packages embed provider-sized raster assets.
