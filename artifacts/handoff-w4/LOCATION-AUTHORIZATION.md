# Location authorization

The Location is taken from the paired session. Pinned Program participation, Staff assignment and
device assignment must all allow the operation. PostgreSQL prevents a device assignment from
exceeding its Staff member and rejects cross-tenant assignment.

