# ADR 0029: Public Wallet asset safety

Google Wallet must fetch some visual assets publicly. W3 publishes only generated, non-PII images through random immutable tokens tied to configuration/projection versions.

The URL does not contain customer, Membership, credential, contact, or tenant database identifiers. Private originals and mutable authorization data remain behind authenticated storage boundaries.
