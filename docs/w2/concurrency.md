# W2 concurrency

Organization advisory locks serialize program capacity, draft-version creation, Test Mode projections, and publication. Draft edits compare the persisted revision and return `STALE_PROGRAM_DRAFT` on conflict. Publish commands are idempotent. Partial database indexes enforce one active editable version and deterministic preview cache keys.
