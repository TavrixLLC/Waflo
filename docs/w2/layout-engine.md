# Layout engine

ROW places stamps linearly. GRID uses bounded columns. PATH snakes alternating rows. RING places stamps around a centered circle. Goals are limited to 2–30 by shared contracts and the renderer. Layout configuration is typed JSON, bounded at the HTTP boundary, and never treated as executable markup.
