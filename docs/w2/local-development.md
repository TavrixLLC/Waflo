# W2 local development

Run the existing PostgreSQL/Redis/Mailpit stack, apply migrations, generate Prisma, then start the API and merchant dashboard with the existing W1 scripts. Local uploads are stored under `tmp/waflo-assets`; generated SVG previews are stored under `tmp/waflo-previews`. No production credentials or Wallet credentials are required.
