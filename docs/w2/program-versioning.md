# Program versioning

New programs begin at version 1 with one draft. The current draft is edited using its revision number. A published version is never edited. Editing a published program creates the next version by copying the published configuration; the published pointer stays active until the new version is published. Older versions become `SUPERSEDED` and remain queryable.
