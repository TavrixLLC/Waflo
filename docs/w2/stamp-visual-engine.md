# Stamp visual engine

`@waflo/stamp-engine` produces deterministic SVG previews from normalized goal, progress, layout, and color inputs. It escapes merchant labels, reports dimensions and a SHA-256 digest, and exposes structural positions for tests. It supports ROW, GRID, PATH, and RING layouts. Progress is clamped to the configured goal.
