# Wallet synchronization

Ledger and projection commit before any provider call. The same transaction upserts coalesced
Wallet state-change work for every active pass instance. Provider failure cannot roll back a
loyalty operation.

Apple changes allocate a global monotonic update sequence and preserve registration semantics.
Google patches the object pinned to the Membership's enrolled Program Version. Test Adapters show
truthful local state; they are not external certification.

Final-ready renders all stamps filled. Final redemption renders the new cycle all empty. No
check, star, gift or number replaces a stamp.

